{
  "name": "wow-class",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "codegen:build": "pnpm ui:codegen && pnpm admin:codegen && pnpm client:codegen && pnpm styled-system:format",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prepare": "husky",
    "ui:codegen": "cd packages/ui && pnpm panda codegen",
    "admin:codegen": "cd apps/admin && pnpm panda codegen",
    "client:codegen": "cd apps/client && pnpm panda codegen",
    "styled-system:format": "prettier --write packages/ui/styled-system/** apps/admin/styled-system/** apps/client/styled-system/**"
  },
  "lint-staged": {
    "**/*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@pandacss/dev": "^0.44.0",
    "@wow-class/panda-config": "workspace:*",
    "husky": "^9.1.4",
    "lint-staged": "^15.2.7",
    "prettier": "^3.2.5",
    "turbo": "^2.0.11",
    "typescript": "^5.4.5",
    "wowds-theme": "^0.1.1"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "wowds-ui": "^0.1.8"
  }
}
